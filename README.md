First-time setup
================

vimproc.vim
-----------

    $ cd ~/.vim/bundle/vimproc.vim
    $ make -f make_unix.mak

clang_complete
--------------

    $ cd ~/.vim/bundle/clang_complete
    $ make install
